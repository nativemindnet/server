disks
монтируем на постоянно, т.к. нужно к ним обращаться по сети и т.д
если не монтировать его постоянно - то сеть не сможет к нему обращаться.

коллизия что если нужно достать диск, то автомаунт может примонтировать его заново





Решение - на диски писать только через smb, в том числе даже если он есть локальный.
тогда загрузчик на диски не нужен в принципе.

Текущий загрузчик disks - не проверяет mount ни делает его.Только проверяет место

-----

smb
перед каждым файлом отмонтируем предыдущий маунт

TODO: в цикле примонтировать хотя бы 1 диск из массива.

пытаемся примонтировать наш маунт
проверяем примонтировался, если нет - то делаем umount, выход
проверяем, есть ли свободное место, если нет - то делаем umount, выход


делаем umount

-----

google

поскольку не ясно, залился ли файл из кеша - нельзя отмонтировать и монтировать постоянно.

ЧТобы проверить подключение и дописать кеш  - залить небольшой файл до и после загрузки (и посмотреть насколько это успешно)


алгоритм

Делаем mount (без сс)
записываем rnd файл
стираем rnd файл
грузим
записывае rnd файл
стираем rnd файл
